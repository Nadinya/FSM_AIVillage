using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum ResourceType
{
    Wood,
    Food,
    Stone
}
public enum Jobs
{
    Worker,
    Breeder,
    Builder,
    Idle,
    Worshipper
}
public enum Gender
{
    Male,
    Female
}
